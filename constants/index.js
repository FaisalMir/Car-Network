{/*this .js file is importing all the variable and constants in the
    constants folder so that it can be used in other files. Doing it 
    this way promotes reusability in the code */}

import assets from "./assets";
import {SIZES, FONTS, COLOURS, SHADOWS} from "./theme";
import { PhotoData } from "./PhotoData";

export {assets, SIZES, FONTS, COLOURS, SHADOWS, PhotoData};